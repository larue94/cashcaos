# Libraries for the agentic paper-trading system.
# Install with:  pip install -r trading/requirements.txt

# Broker connection (Alpaca paper trading)
alpaca-py>=0.33.0

# Reads settings (your API keys) from the .env file
python-dotenv>=1.0.0

# --- Used from Phase 2 onward (installed now so setup is one-time) ---
# Market data & fundamentals
yfinance>=0.2.50
pandas>=2.0.0
numpy>=1.26.0
requests>=2.31.0

# LLM reasoning layer (Claude API)
anthropic>=0.40.0

# The clickable local web app (python -m trading.webapp)
flask>=3.0.0

# Chart images for Telegram / reports
matplotlib>=3.8.0

# HTML table parsing (insider-buying feed)
lxml>=5.0.0
